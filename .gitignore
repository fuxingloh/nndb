# Rust
/nndb/target/
**/*.rs.bk

# Datasets (large; fetched via nndb/scripts/download-sift.sh)
/nndb/data/

# Web
/website/node_modules/
/website/.next/
/website/dist/

# Misc
.DS_Store
.idea/

# deepsec vulnerability scanner — local working dir, never committed
/.deepsec/

# Secrets / credentials — NEVER commit (AWS creds, tokens, keys)
.env
.env.*
!.env.example
*.pem
*.p12
*.key
**/credentials
**/aws-credentials*

# hardware sweep raw per-box logs (notes/062*.json is canonical)
notes/hw/
